coverage:
  status:
    project:
      default:
        target: auto
        threshold: 2%
    patch:
      default:
        target: auto

comment:
  layout: "reach, diff, flags, files"
  require_changes: false

flags:
  backend:
    paths:
      - apps/backend/
